{
  "routes": [
    {
      "method": "GET",
      "path": "/me",
      "handler": "event-manager.me",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/orders/:slug",
      "handler": "event-manager.orders",
      "config": {
        "policies": []
      }
    }
  ]
}
