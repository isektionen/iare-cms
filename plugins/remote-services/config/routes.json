{
  "routes": [
    {
      "method": "POST",
      "path": "/init",
      "handler": "remote-services.issueBearer",
      "config": {
        "policies": ["plugins::users-permissions.ratelimit"]
      }
    }
  ]
}
